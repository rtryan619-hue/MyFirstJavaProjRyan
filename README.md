# MyFirstJavaProjRyan
This lab practices file reading and exception handling in Java. The program uses a File object and Scanner to read student grades from grades.txt and display them line by line. It also uses a try-catch block to handle the case where the file is missing, printing "File not found!" instead of crashing.
